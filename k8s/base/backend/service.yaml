apiVersion: v1
kind: Service
metadata:
  name: splitzy-backend
  labels:
    app.kubernetes.io/name: splitzy-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: splitzy-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
